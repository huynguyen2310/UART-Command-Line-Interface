#include "gettemp.h"

void getTemp(char **argv, uint8_t argnum){
	//				for(uint8_t i =0; i < index; i++){
//					HAL_UART_Transmit(&huart2, argv[i], strlen((char*)argv[i]),100);
//					HAL_UART_Transmit(&huart2, (uint8_t*)"\n", 1,100);
}